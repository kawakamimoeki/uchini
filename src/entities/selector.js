/**
 * SelectorUsage
 */
export class Selector {
  /**
   * @param {string} name
   * @param {Array} positions
   */
  constructor (name, positions) {
    this.name = name
    this.positions = positions
  }
}
